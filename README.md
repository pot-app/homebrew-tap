# homebrew-tap

## Install with brew

- Add our tap:

```bash
brew tap pot-app/homebrew-tap
```

- Install pot:

```bash
brew install --cask pot
```

- Upgrade pot

```bash
brew upgrade --cask pot
```
